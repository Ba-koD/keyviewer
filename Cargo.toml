[workspace]
members = ["src-tauri"]
default-members = ["src-tauri"]
resolver = "2"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "z"
strip = true

[profile.ci-release]
inherits = "release"
lto = "thin"
codegen-units = 16
opt-level = 2
