# 변경 이력 (Changelog)

## v1.1.6 (최신)

### ✨ 새로운 기능
- **macOS / Linux 기능 동등성** - Windows 전용이던 기능을 세 플랫폼 모두에서 지원
  - Linux 설정 저장 (`~/.config/keyviewer/settings.json`) - 포트, 언어, 타겟, 오버레이 설정
  - 시작 프로그램 등록 - macOS LaunchAgent, Linux XDG autostart
  - 자동 업데이트 - 플랫폼·아키텍처별 릴리즈 에셋 선택 및 설치
  - 포트 점유 프로세스 조회 및 종료 - `lsof` / `ss` 기반, TERM → KILL 단계적 종료
- **Wayland 지원**
  - 키 입력 캡처 - `/dev/input` 기반 커널 레벨 백엔드 (X11, Wayland, TTY 공통)
  - 활성 창 감지 - Hyprland, sway, GNOME, KDE 경로 지원
- **앱 내 로그 패널** - Windows 콘솔 창 대신 앱이 자체 출력을 캡처해 표시, 전 플랫폼 동작
- **키 입력 실패 안내** - 권한이나 세션 문제로 키를 잡지 못할 때 원인과 해결 방법 표시

### 🔧 개선사항
- 릴리즈 빌드 로그 확인 가능 - GUI 바이너리라 모든 출력이 유실되던 문제 해소
- Wayland 활성 창 조회에 150ms 캐시 적용 - 키 입력마다 프로세스를 생성하던 문제 방지
- "Windows 시작 시 실행" → "시스템 시작 시 실행"으로 명칭 변경

### 🐛 버그 수정
- Linux 창 정보의 프로세스명이 항상 비어 있어 "프로세스" 타겟 모드가 동작하지 않던 문제
- Linux 창 목록이 항상 비어 있던 문제 (`_NET_CLIENT_LIST` 사용)
- X11에서 포커스가 자식 창일 때 제목과 클래스를 읽지 못하던 문제
- macOS 설정 초기화가 그라디언트 및 오버레이 모드 관련 키 5개를 삭제하지 않던 문제

---

## v1.1.5

### ✨ 새로운 기능
- **서버 상태 트레이 표시** - 서버 실행 중이면 트레이 아이콘 우하단에 빨간 점 표시
- **작업 표시줄 배지** - Windows 작업 표시줄 버튼에 오버레이 배지 (macOS/Linux는 dock·런처 배지)
- **트레이 상시 표시** - 창이 열려 있어도 트레이 아이콘 유지, 좌클릭으로 창 표시/숨김 전환
- 트레이 툴팁에 서버 상태와 포트 표시, 메뉴 항목이 상태에 따라 활성화
- 트레이 메뉴 한국어/영어 지원

### 🔧 개선사항
- 트레이 아이콘 DPI 대응 - 화면 배율에 맞는 크기를 아이콘에서 직접 선택

### 🐛 버그 수정
- OBS 로컬 내보내기 파일의 설정이 고정되지 않던 문제

---

## v1.1.4

### 🔧 개선사항
- 업데이트 안내 문구 개선
- pre-push 훅 빌드 캐시 도입으로 검사 시간 단축

---

## v1.1.3

### 🐛 버그 수정
- OBS 로컬 내보내기 파일이 내보낸 시점의 스타일·이미지 설정을 유지하지 않던 문제

---

## v1.1.2

### ✨ 새로운 기능
- **파일별 OBS 타겟 필터** - 내보낸 오버레이 파일마다 다른 타겟 조건 적용 가능

### 🔧 개선사항
- 릴리즈 빌드를 플랫폼별로 병렬 실행
- 저장소 루트에서 `cargo run` 지원

### 🐛 버그 수정
- Windows 시작 시 실행 동작이 정상적으로 등록되지 않던 문제
- 런처 시작 동작 복구

---

## v1.1.1

### ✨ 새로운 기능
- **자동 업데이트** - GitHub 릴리즈를 확인해 앱 내에서 최신 버전 설치 (Windows)

### 🔧 개선사항
- 런처 업데이트 UI 간소화
- 동일 버전 릴리즈 재발행 시 기존 릴리즈 교체

### 🐛 버그 수정
- OBS 로컬 HTML 내보내기가 현재 설정과 어긋나던 문제

---

## v1.1.0

### ✨ 새로운 기능
- **OBS 로컬 파일 내보내기** - 단일 HTML로 내보내 브라우저 소스에서 로컬 파일로 사용
  - 내보내는 시점의 오버레이·이미지·스타일 설정을 파일에 내장
  - 키 텍스트 숨김 설정도 함께 저장
- **Key Viewer 모드** - 큐 방식 외에 고정 배치 방식 추가
- **키 이미지 편집 및 텍스트 스타일** - 키별 이미지 지정과 텍스트 스타일 세부 조정
- **클라우드 설정 동기화** - Cloudflare Worker 기반 OAuth 인증, 내보내기 진행률 표시
- **Windows 관리자 권한 확인 및 콘솔 표시 제어**

### 🔧 개선사항
- 런처 및 컨트롤 패널 UI/UX 전반 개선
- `version.txt` 기반 버전 자동 동기화
- pre-push Git 훅 도입 - 변경분이 있을 때만 fmt·clippy·test 실행
- GitHub Actions 재정비 - 테스트와 빌드 워크플로 분리, 캐시 최적화
- Windows 프로세스 목록 조회 로직 개선
- 아키텍처 문서 및 에이전트 지침 추가

### 🐛 버그 수정
- 텍스트 투명도가 칩 전체에 적용되던 문제 (텍스트에만 적용되도록 수정)
- 오버레이에서 키 이미지가 표시되지 않던 문제
- macOS/Linux 크로스 플랫폼 빌드 경고 및 clippy 오류

---

## v1.0.4

### ✨ 새로운 기능
- **설정 초기화 버튼** 추가 - 런처에서 모든 설정을 기본값으로 초기화 가능
- **Windows 레지스트리 기반 설정 저장**
  - 포트, 언어, 자동 시작 설정
  - 타겟 모드 및 선택값
  - 오버레이 모든 설정 (색상, 레이아웃, 애니메이션 등)
- **포트 충돌 감지 및 상세 오류 메시지**
  - 포트가 이미 사용 중인 경우 감지
  - 권한 부족 시 상세한 안내 메시지

### 🔧 개선사항
- **OBS 브라우저 소스 캐싱 문제 해결**
  - `no-cache` HTTP 헤더 추가
  - 더 이상 OBS에서 수동 캐시 초기화 불필요
- **자동 프로세스 모드 전환**
  - 창 리스트에서 클릭 시 자동으로 "프로세스" 모드로 변경
  - "사용 안 함" 상태에서도 즉시 활성화
- **UI 파일 실행 파일 내장**
  - HTML/CSS 파일이 exe에 임베드
  - Portable 버전이 진짜 단일 파일로 동작

### 🐛 버그 수정
- 설정 파일(JSON) 생성 제거 - 모든 설정이 레지스트리에 저장
- PowerShell 5.x 호환성 개선 (삼항 연산자 제거)

---

## v1.0.0-1.0.3

### 🎉 Tauri 마이그레이션
- Python/PyInstaller → Rust/Tauri 완전 재작성
- 파일 크기 90% 감소 (~80MB → ~8MB)
- 메모리 사용량 70% 감소 (~100MB → ~30MB)
- 바이러스 오탐 대폭 감소

### 주요 기능
- 실시간 키보드 입력 모니터링
- 웹 기반 컨트롤 패널 및 오버레이
- 창 타겟팅 (제목, 프로세스, HWND, 클래스명, 전체)
- OBS Browser Source 지원
- 커스터마이즈 가능한 오버레이
- 다국어 지원 (한국어/English)

### 크로스 플랫폼 지원
- Windows (MSI, NSIS, Portable)
- macOS (Intel, Apple Silicon)
- Linux (AppImage, Deb)

---

## 개발자 변경사항

### 빌드 시스템
- **GitHub Actions 개선**
  - 각 플랫폼 빌드를 별도 job으로 분리
  - 실패한 빌드가 있어도 성공한 것만 릴리스
  - 빌드 결과 체크리스트 자동 생성
  - 성공한 파일만 첨부

### 아키텍처
- **설정 저장 방식 변경**
  - 파일 시스템 → Windows 레지스트리
  - 레지스트리 위치: `HKEY_CURRENT_USER\Software\KeyViewer`
- **HTTP 캐시 헤더**
  - 모든 정적 리소스에 `Cache-Control: no-cache` 추가
  - OBS 브라우저 소스 호환성 향상

### 코드 개선
- Rust `u16` ↔ `u32` 변환 (레지스트리 호환)
- 사용하지 않는 import 제거 (`Html`)
- 포트 바인딩 전 가용성 체크

---

## 알려진 이슈

### Windows
- 관리자 권한 필요 (전역 키보드 후킹)
- 1024 미만 포트는 권한 문제 가능성

### macOS
- 접근성 권한 필요
- Gatekeeper 경고 (코드 서명 필요)

### Linux
- 전역 키 입력 캡처에 `input` 그룹 권한 필요 (`sudo usermod -aG input $USER` 후 재로그인)
- Wayland 창 타겟팅은 컴포지터에 따라 추가 구성 필요
  - Hyprland, sway: 별도 설정 없이 동작
  - GNOME: `window-calls-extended` 셸 확장 필요
  - KDE: `kdotool` 설치 필요

---

**전체 변경사항**: [GitHub Releases](https://github.com/Ba-koD/keyviewer/releases)

